<div class="admin-dashboard">
  <div class="admin-header">
    <%= image_tag "energia-pilates/1. logotipo/EP_logotipo 1.png", alt: "Energía Pilates", class: "admin-logo" %>
  </div>

  <!-- Estadísticas Rápidas -->
  <div class="admin-stats">
    <%= link_to management_attendance_path, class: "stat-card stat-card--link" do %>
      <div class="stat-number">✓</div>
      <div class="stat-label">Asistencia</div>
    <% end %>

    <%= link_to management_students_path, class: "stat-card stat-card--link" do %>
      <div class="stat-number"><%= @students_active_count %></div>
      <div class="stat-label">Alumnos</div>
    <% end %>

    <%= link_to management_classes_path, class: "stat-card stat-card--link" do %>
      <div class="stat-number"><%= @classes_today_count %></div>
      <div class="stat-label">Calendario</div>
    <% end %>

    <%= link_to management_requests_path, class: "stat-card stat-card--link" do %>
      <div class="stat-number"><%= @pending_requests_count %></div>
      <div class="stat-label">Solicitudes Pendientes</div>
    <% end %>

    <%= link_to management_classes_path, class: "stat-card stat-card--link" do %>
      <div class="stat-number"><%= @classes_count %></div>
      <div class="stat-label">Clases</div>
    <% end %>
  </div>

  <!-- Accesos principales -->
  <div class="admin-quick-access">
    <h2>Accesos</h2>
    <div class="quick-access-grid">
      <% if current_user.admin? %>
        <%= link_to management_attendance_path, class: "quick-access-card" do %>
          <div class="card-icon">📋</div>
          <h3>Asistencia</h3>
          <p>Tomar lista por sala, fecha y horario</p>
        <% end %>

        <%= link_to new_management_class_path, class: "quick-access-card" do %>
          <div class="card-icon">🗓️</div>
          <h3>Nueva clase</h3>
          <p>Crear una clase rápido</p>
        <% end %>

        <%= link_to management_students_path, class: "quick-access-card" do %>
          <div class="card-icon">👥</div>
          <h3>Alumnos</h3>
          <p>Listado por nombre y ficha completa</p>
        <% end %>

        <%= link_to new_management_student_path, class: "quick-access-card" do %>
          <div class="card-icon">➕</div>
          <h3>Nuevo alumno</h3>
          <p>Alta rápida en el sistema</p>
        <% end %>

        <%= link_to debtors_management_students_path, class: "quick-access-card" do %>
          <div class="card-icon">💰</div>
          <h3>Deudores</h3>
          <p>Alumnos con deuda y montos</p>
        <% end %>

        <%= link_to absences_management_students_path, class: "quick-access-card" do %>
          <div class="card-icon">📵</div>
          <h3>Contactar inasistencias</h3>
          <p>Más de 2 cancelaciones en el mes</p>
        <% end %>

        <%= link_to birthdays_management_students_path, class: "quick-access-card" do %>
          <div class="card-icon">🎂</div>
          <h3>Cumpleaños</h3>
          <p>Listado para contactar / automatizar</p>
        <% end %>

        <%= link_to management_cashbox_path, class: "quick-access-card" do %>
          <div class="card-icon">💵</div>
          <h3>Caja</h3>
          <p>Pagos por período y método</p>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

